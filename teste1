-- Script de Compartilhamento de Servidor Privado
-- Coloque este script em StarterPlayer > StarterPlayerScripts

local TeleportService = game:GetService("TeleportService")
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local player = Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Criar GUI
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ServerShareGUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- Frame principal
local mainFrame = Instance.new("Frame")
mainFrame.Size = UDim2.new(0, 400, 0, 250)
mainFrame.Position = UDim2.new(0.5, -200, 0.5, -125)
mainFrame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui

local corner = Instance.new("UICorner")
corner.CornerRadius = UDim.new(0, 10)
corner.Parent = mainFrame

-- Título
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -20, 0, 40)
title.Position = UDim2.new(0, 10, 0, 10)
title.BackgroundTransparency = 1
title.Text = "🔗 Compartilhar Servidor Privado"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextSize = 18
title.Font = Enum.Font.GothamBold
title.Parent = mainFrame

-- Campo de texto para link
local linkBox = Instance.new("TextBox")
linkBox.Size = UDim2.new(1, -40, 0, 40)
linkBox.Position = UDim2.new(0, 20, 0, 60)
linkBox.BackgroundColor3 = Color3.fromRGB(50, 50, 50)
linkBox.BorderSizePixel = 0
linkBox.PlaceholderText = "Cole o link do servidor privado aqui..."
linkBox.Text = ""
linkBox.TextColor3 = Color3.fromRGB(255, 255, 255)
linkBox.TextSize = 14
linkBox.Font = Enum.Font.Gotham
linkBox.ClearTextOnFocus = false
linkBox.Parent = mainFrame

local linkCorner = Instance.new("UICorner")
linkCorner.CornerRadius = UDim.new(0, 6)
linkCorner.Parent = linkBox

-- Botão compartilhar
local shareButton = Instance.new("TextButton")
shareButton.Size = UDim2.new(1, -40, 0, 45)
shareButton.Position = UDim2.new(0, 20, 0, 115)
shareButton.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
shareButton.BorderSizePixel = 0
shareButton.Text = "📤 COMPARTILHAR COM TODOS"
shareButton.TextColor3 = Color3.fromRGB(255, 255, 255)
shareButton.TextSize = 16
shareButton.Font = Enum.Font.GothamBold
shareButton.Parent = mainFrame

local btnCorner = Instance.new("UICorner")
btnCorner.CornerRadius = UDim.new(0, 8)
btnCorner.Parent = shareButton

-- Status
local statusLabel = Instance.new("TextLabel")
statusLabel.Size = UDim2.new(1, -40, 0, 30)
statusLabel.Position = UDim2.new(0, 20, 0, 170)
statusLabel.BackgroundTransparency = 1
statusLabel.Text = "Digite o link e clique em compartilhar"
statusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
statusLabel.TextSize = 12
statusLabel.Font = Enum.Font.Gotham
statusLabel.Parent = mainFrame

-- Botão fechar
local closeButton = Instance.new("TextButton")
closeButton.Size = UDim2.new(0, 30, 0, 30)
closeButton.Position = UDim2.new(1, -40, 0, 10)
closeButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
closeButton.BorderSizePixel = 0
closeButton.Text = "X"
closeButton.TextColor3 = Color3.fromRGB(255, 255, 255)
closeButton.TextSize = 18
closeButton.Font = Enum.Font.GothamBold
closeButton.Parent = mainFrame

local closeCorner = Instance.new("UICorner")
closeCorner.CornerRadius = UDim.new(0, 6)
closeCorner.Parent = closeButton

-- Botão toggle (minimizar/maximizar)
local toggleButton = Instance.new("TextButton")
toggleButton.Size = UDim2.new(0, 120, 0, 35)
toggleButton.Position = UDim2.new(0, 10, 0, 10)
toggleButton.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
toggleButton.BorderSizePixel = 0
toggleButton.Text = "🔗 Servidor"
toggleButton.TextColor3 = Color3.fromRGB(255, 255, 255)
toggleButton.TextSize = 14
toggleButton.Font = Enum.Font.GothamBold
toggleButton.Visible = false
toggleButton.Parent = screenGui

local toggleCorner = Instance.new("UICorner")
toggleCorner.CornerRadius = UDim.new(0, 8)
toggleCorner.Parent = toggleButton

-- Funções
local function updateStatus(text, color)
	statusLabel.Text = text
	statusLabel.TextColor3 = color or Color3.fromRGB(200, 200, 200)
end

local function extractServerCode(link)
	-- Extrair código do servidor privado do link
	local patterns = {
		"privateServerLinkCode=([^&]+)",
		"code=([^&]+)",
		"/share%?code=([^&]+)"
	}
	
	for _, pattern in ipairs(patterns) do
		local code = string.match(link, pattern)
		if code then
			return code
		end
	end
	
	return nil
end

local function shareServerLink()
	local link = linkBox.Text
	
	if link == "" or link == nil then
		updateStatus("❌ Por favor, insira um link!", Color3.fromRGB(255, 100, 100))
		return
	end
	
	local serverCode = extractServerCode(link)
	
	if not serverCode then
		updateStatus("❌ Link inválido! Verifique o formato.", Color3.fromRGB(255, 100, 100))
		return
	end
	
	updateStatus("📡 Compartilhando com todos os jogadores...", Color3.fromRGB(100, 255, 100))
	
	-- Enviar para o chat
	local ReplicatedStorage = game:GetService("ReplicatedStorage")
	local chatMessage = "🎮 SERVIDOR PRIVADO DISPONÍVEL! 🎮\n" .. link .. "\n🚀 Clique para entrar!"
	
	-- Tentar enviar mensagem no chat
	pcall(function()
		game:GetService("TextChatService").TextChannels.RBXGeneral:SendAsync(chatMessage)
	end)
	
	-- Método alternativo (chat legado)
	pcall(function()
		game:GetService("ReplicatedStorage").DefaultChatSystemChatEvents.SayMessageRequest:FireServer(chatMessage, "All")
	end)
	
	-- Contar jogadores notificados
	local playerCount = #Players:GetPlayers() - 1
	updateStatus("✅ Compartilhado com " .. playerCount .. " jogador(es)!", Color3.fromRGB(100, 255, 100))
	
	wait(2)
	updateStatus("Link copiado! Envie para seus amigos.", Color3.fromRGB(200, 200, 200))
end

-- Eventos dos botões
shareButton.MouseButton1Click:Connect(shareServerLink)

closeButton.MouseButton1Click:Connect(function()
	mainFrame.Visible = false
	toggleButton.Visible = true
end)

toggleButton.MouseButton1Click:Connect(function()
	mainFrame.Visible = true
	toggleButton.Visible = false
end)

-- Efeito hover no botão compartilhar
shareButton.MouseEnter:Connect(function()
	shareButton.BackgroundColor3 = Color3.fromRGB(0, 200, 255)
end)

shareButton.MouseLeave:Connect(function()
	shareButton.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
end)

-- Copiar link ao clicar na caixa de texto
linkBox.Focused:Connect(function()
	updateStatus("💡 Cole seu link aqui com Ctrl+V", Color3.fromRGB(255, 200, 100))
end)

print("✅ Sistema de Compartilhamento de Servidor Privado carregado!")
print("📝 Cole o link do seu servidor privado e compartilhe com todos!")
