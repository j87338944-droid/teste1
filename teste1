-- Steal a Brainrot Auto Server Finder & Position Saver
-- Criado para executores Roblox

local Players = game:GetService("Players")
local TeleportService = game:GetService("TeleportService")
local HttpService = game:GetService("HttpService")
local LocalPlayer = Players.LocalPlayer

-- Configurações
local GAME_ID = 109983668079237
local savedPosition = nil
local isSearching = false

-- Criar GUI
local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local TitleLabel = Instance.new("TextLabel")
local ServersTab = Instance.new("Frame")
local PositionTab = Instance.new("Frame")
local NextButton = Instance.new("TextButton")
local BackButton = Instance.new("TextButton")
local ScrollFrame = Instance.new("ScrollingFrame")
local UIListLayout = Instance.new("UIListLayout")

-- Botões da aba de posição
local SavePosButton = Instance.new("TextButton")
local TeleportBackButton = Instance.new("TextButton")
local StatusLabel = Instance.new("TextLabel")

-- Botão de busca rápida
local QuickSearchButton = Instance.new("TextButton")

ScreenGui.Name = "BrainrotFinderGUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = game:GetService("CoreGui")

-- Configurar Frame Principal
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 500, 0, 400)
MainFrame.Position = UDim2.new(0.5, -250, 0.5, -200)
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Parent = ScreenGui

-- Adicionar borda arredondada
local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(0, 10)
Corner.Parent = MainFrame

-- Título
TitleLabel.Name = "Title"
TitleLabel.Size = UDim2.new(1, 0, 0, 50)
TitleLabel.BackgroundColor3 = Color3.fromRGB(20, 20, 25)
TitleLabel.BorderSizePixel = 0
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = "🧠 BRAINROT FINDER"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 20
TitleLabel.Parent = MainFrame

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 10)
TitleCorner.Parent = TitleLabel

-- ABA DE SERVIDORES
ServersTab.Name = "ServersTab"
ServersTab.Size = UDim2.new(1, -20, 1, -70)
ServersTab.Position = UDim2.new(0, 10, 0, 60)
ServersTab.BackgroundTransparency = 1
ServersTab.Visible = true
ServersTab.Parent = MainFrame

-- Botão de Busca Rápida
QuickSearchButton.Name = "QuickSearch"
QuickSearchButton.Size = UDim2.new(0, 200, 0, 40)
QuickSearchButton.Position = UDim2.new(0.5, -100, 0, 10)
QuickSearchButton.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
QuickSearchButton.BorderSizePixel = 0
QuickSearchButton.Font = Enum.Font.GothamBold
QuickSearchButton.Text = "⚡ BUSCAR RÁPIDO"
QuickSearchButton.TextColor3 = Color3.fromRGB(255, 255, 255)
QuickSearchButton.TextSize = 16
QuickSearchButton.Parent = ServersTab

local QuickCorner = Instance.new("UICorner")
QuickCorner.CornerRadius = UDim.new(0, 8)
QuickCorner.Parent = QuickSearchButton

-- ScrollFrame para lista de servidores
ScrollFrame.Name = "ServerList"
ScrollFrame.Size = UDim2.new(1, -10, 1, -70)
ScrollFrame.Position = UDim2.new(0, 5, 0, 60)
ScrollFrame.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
ScrollFrame.BorderSizePixel = 0
ScrollFrame.ScrollBarThickness = 8
ScrollFrame.Parent = ServersTab

local ScrollCorner = Instance.new("UICorner")
ScrollCorner.CornerRadius = UDim.new(0, 8)
ScrollCorner.Parent = ScrollFrame

UIListLayout.Parent = ScrollFrame
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 5)

-- ABA DE POSIÇÃO
PositionTab.Name = "PositionTab"
PositionTab.Size = UDim2.new(1, -20, 1, -70)
PositionTab.Position = UDim2.new(0, 10, 0, 60)
PositionTab.BackgroundTransparency = 1
PositionTab.Visible = false
PositionTab.Parent = MainFrame

-- Status Label
StatusLabel.Name = "Status"
StatusLabel.Size = UDim2.new(1, -20, 0, 60)
StatusLabel.Position = UDim2.new(0, 10, 0, 10)
StatusLabel.BackgroundColor3 = Color3.fromRGB(40, 40, 45)
StatusLabel.BorderSizePixel = 0
StatusLabel.Font = Enum.Font.Gotham
StatusLabel.Text = "Nenhuma posição salva"
StatusLabel.TextColor3 = Color3.fromRGB(200, 200, 200)
StatusLabel.TextSize = 14
StatusLabel.TextWrapped = true
StatusLabel.Parent = PositionTab

local StatusCorner = Instance.new("UICorner")
StatusCorner.CornerRadius = UDim.new(0, 8)
StatusCorner.Parent = StatusLabel

-- Botão Salvar Posição
SavePosButton.Name = "SavePos"
SavePosButton.Size = UDim2.new(0, 200, 0, 50)
SavePosButton.Position = UDim2.new(0.5, -100, 0, 90)
SavePosButton.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
SavePosButton.BorderSizePixel = 0
SavePosButton.Font = Enum.Font.GothamBold
SavePosButton.Text = "💾 SALVAR POSIÇÃO"
SavePosButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SavePosButton.TextSize = 16
SavePosButton.Parent = PositionTab

local SaveCorner = Instance.new("UICorner")
SaveCorner.CornerRadius = UDim.new(0, 8)
SaveCorner.Parent = SavePosButton

-- Botão Voltar Posição
TeleportBackButton.Name = "TeleportBack"
TeleportBackButton.Size = UDim2.new(0, 200, 0, 50)
TeleportBackButton.Position = UDim2.new(0.5, -100, 0, 160)
TeleportBackButton.BackgroundColor3 = Color3.fromRGB(200, 100, 0)
TeleportBackButton.BorderSizePixel = 0
TeleportBackButton.Font = Enum.Font.GothamBold
TeleportBackButton.Text = "📍 VOLTAR POSIÇÃO"
TeleportBackButton.TextColor3 = Color3.fromRGB(255, 255, 255)
TeleportBackButton.TextSize = 16
TeleportBackButton.Parent = PositionTab

local TeleportCorner = Instance.new("UICorner")
TeleportCorner.CornerRadius = UDim.new(0, 8)
TeleportCorner.Parent = TeleportBackButton

-- Botões de Navegação
NextButton.Name = "Next"
NextButton.Size = UDim2.new(0, 100, 0, 35)
NextButton.Position = UDim2.new(1, -110, 1, -45)
NextButton.BackgroundColor3 = Color3.fromRGB(100, 100, 255)
NextButton.BorderSizePixel = 0
NextButton.Font = Enum.Font.GothamBold
NextButton.Text = "→ PRÓXIMO"
NextButton.TextColor3 = Color3.fromRGB(255, 255, 255)
NextButton.TextSize = 14
NextButton.Parent = MainFrame

local NextCorner = Instance.new("UICorner")
NextCorner.CornerRadius = UDim.new(0, 6)
NextCorner.Parent = NextButton

BackButton.Name = "Back"
BackButton.Size = UDim2.new(0, 100, 0, 35)
BackButton.Position = UDim2.new(0, 10, 1, -45)
BackButton.BackgroundColor3 = Color3.fromRGB(100, 100, 255)
BackButton.BorderSizePixel = 0
BackButton.Font = Enum.Font.GothamBold
BackButton.Text = "← VOLTAR"
BackButton.TextColor3 = Color3.fromRGB(255, 255, 255)
BackButton.TextSize = 14
BackButton.Visible = false
BackButton.Parent = MainFrame

local BackCorner = Instance.new("UICorner")
BackCorner.CornerRadius = UDim.new(0, 6)
BackCorner.Parent = BackButton

-- Funções
local function createServerButton(serverData)
    local Button = Instance.new("TextButton")
    Button.Size = UDim2.new(1, -10, 0, 60)
    Button.BackgroundColor3 = Color3.fromRGB(50, 50, 60)
    Button.BorderSizePixel = 0
    Button.Font = Enum.Font.Gotham
    Button.TextColor3 = Color3.fromRGB(255, 255, 255)
    Button.TextSize = 14
    Button.TextXAlignment = Enum.TextXAlignment.Left
    Button.TextYAlignment = Enum.TextYAlignment.Top
    
    local brainrotText = serverData.brainrot or "Desconhecido"
    local pingText = serverData.ping or "?"
    local playersText = serverData.playing .. "/" .. serverData.maxPlayers
    
    Button.Text = string.format("  🧠 Brainrot: %s\n  👥 Players: %s | 📶 Ping: %sms", 
        brainrotText, playersText, pingText)
    
    local ButtonCorner = Instance.new("UICorner")
    ButtonCorner.CornerRadius = UDim.new(0, 6)
    ButtonCorner.Parent = Button
    
    Button.MouseButton1Click:Connect(function()
        Button.Text = "  ⏳ Entrando no servidor..."
        Button.BackgroundColor3 = Color3.fromRGB(100, 200, 100)
        wait(1)
        TeleportService:TeleportToPlaceInstance(GAME_ID, serverData.id, LocalPlayer)
    end)
    
    return Button
end

local function searchServers()
    if isSearching then return end
    isSearching = true
    
    QuickSearchButton.Text = "⏳ BUSCANDO..."
    QuickSearchButton.BackgroundColor3 = Color3.fromRGB(100, 100, 100)
    
    -- Limpar lista atual
    for _, child in pairs(ScrollFrame:GetChildren()) do
        if child:IsA("TextButton") then
            child:Destroy()
        end
    end
    
    -- Simular busca de servidores (substitua com API real se disponível)
    task.spawn(function()
        local mockServers = {
            {id = "1", playing = 8, maxPlayers = 12, brainrot = "60m", ping = 45},
            {id = "2", playing = 10, maxPlayers = 12, brainrot = "50m", ping = 78},
            {id = "3", playing = 6, maxPlayers = 12, brainrot = "60m", ping = 32},
            {id = "4", playing = 11, maxPlayers = 12, brainrot = "50m", ping = 91},
            {id = "5", playing = 5, maxPlayers = 12, brainrot = "60m", ping = 55},
        }
        
        for _, serverData in ipairs(mockServers) do
            local button = createServerButton(serverData)
            button.Parent = ScrollFrame
            wait(0.1)
        end
        
        QuickSearchButton.Text = "✅ " .. #mockServers .. " SERVIDORES"
        QuickSearchButton.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
        wait(2)
        QuickSearchButton.Text = "⚡ BUSCAR RÁPIDO"
        QuickSearchButton.BackgroundColor3 = Color3.fromRGB(0, 170, 255)
        isSearching = false
    end)
end

-- Eventos dos Botões
QuickSearchButton.MouseButton1Click:Connect(searchServers)

SavePosButton.MouseButton1Click:Connect(function()
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        savedPosition = LocalPlayer.Character.HumanoidRootPart.CFrame
        StatusLabel.Text = "✅ Posição salva!\n" .. tostring(savedPosition.Position)
        StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
        SavePosButton.BackgroundColor3 = Color3.fromRGB(0, 255, 100)
        wait(1)
        SavePosButton.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
    else
        StatusLabel.Text = "❌ Erro: Personagem não encontrado!"
        StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
    end
end)

TeleportBackButton.MouseButton1Click:Connect(function()
    if savedPosition then
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            local hrp = LocalPlayer.Character.HumanoidRootPart
            local humanoid = LocalPlayer.Character:FindFirstChild("Humanoid")
            
            -- Desabilitar física temporariamente
            if humanoid then
                humanoid:ChangeState(Enum.HumanoidStateType.Physics)
            end
            
            -- Teleportar usando diferentes métodos para maior compatibilidade
            hrp.CFrame = savedPosition
            hrp.Anchored = true
            task.wait(0.1)
            hrp.Anchored = false
            
            if humanoid then
                humanoid:ChangeState(Enum.HumanoidStateType.GettingUp)
            end
            
            StatusLabel.Text = "✅ Teleportado de volta!"
            StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
            TeleportBackButton.BackgroundColor3 = Color3.fromRGB(0, 255, 100)
            wait(1)
            TeleportBackButton.BackgroundColor3 = Color3.fromRGB(200, 100, 0)
        else
            StatusLabel.Text = "❌ Erro: Personagem não encontrado!"
            StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
        end
    else
        StatusLabel.Text = "❌ Nenhuma posição salva ainda!"
        StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
    end
end)

NextButton.MouseButton1Click:Connect(function()
    ServersTab.Visible = false
    PositionTab.Visible = true
    NextButton.Visible = false
    BackButton.Visible = true
end)

BackButton.MouseButton1Click:Connect(function()
    ServersTab.Visible = true
    PositionTab.Visible = false
    NextButton.Visible = true
    BackButton.Visible = false
end)

-- Iniciar busca automática ao carregar
wait(1)
searchServers()

print("✅ Brainrot Finder carregado com sucesso!")
